# Required — Xavier connection
XAVIER_URL=https://clawdbot-railway-template-production-e8ba.up.railway.app
XAVIER_TAILSCALE_URL=https://xavier.YOUR_TAILNET.ts.net
SETUP_PASSWORD=your_xavier_setup_password

# Required — Telegram alerting
TELEGRAM_BOT_TOKEN=your_bot_token
TELEGRAM_ALERT_CHAT_ID=your_telegram_chat_id

# Required — Dashboard auth
MONITOR_USERNAME=admin
MONITOR_PASSWORD=change_me

# Required — Tailscale
TS_AUTHKEY=tskey-auth-xxxxx
TS_HOSTNAME=xavier-monitor

# Required — Gateway WebSocket connection (device-token auth for operator scopes)
GATEWAY_DEVICE_ID=your_device_id_from_paired.json
GATEWAY_DEVICE_TOKEN=your_device_token_from_paired.json
GATEWAY_DEVICE_PUBKEY=your_device_pubkey_base64url
GATEWAY_DEVICE_PRIVKEY=your_device_privkey_base64url
# Fallback: basic gateway token (limited scopes, no sessions/files access)
# GATEWAY_TOKEN=your_gateway_token_from_openclaw.json_gateway.auth.token
# XAVIER_TAILSCALE_HOST=xavier
# GATEWAY_PORT=18789

# Optional (with defaults)
# PORT=9090
# HEALTH_INTERVAL_MS=30000
# LOG_INTERVAL_MS=60000
# DEBUG_INTERVAL_MS=300000
